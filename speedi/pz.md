
---

# ⚡ Speedi — Тестер скорости интернет-соединения

## 🧠 Пояснительная записка

### к shell-утилите для измерения скорости соединения через **Cloudflare Speed Test API**

---

### 📌 Назначение скрипта

**Speedi** — это лёгкая консольная утилита, написанная на **Bash**, для измерения **скорости загрузки и отправки данных** через официальный сервис [speed.cloudflare.com](https://speed.cloudflare.com).
Скрипт подходит для быстрой диагностики интернет-подключения без графического интерфейса и лишних зависимостей.

---

### ⚙️ Ключевые возможности

✅ Измеряет **скорость загрузки (download)**
✅ Измеряет **скорость отправки (upload)**
✅ Проверяет наличие доступа к интернету
✅ Проверяет наличие необходимых инструментов (`curl`, `bc`)
✅ Работает в **любой POSIX-совместимой оболочке (bash/zsh)**
✅ Выводит результаты в удобном читаемом виде (Mbit/s)

---

### 🔧 Основные функции

| Функция                       | Назначение                                     |
| ----------------------------- | ---------------------------------------------- |
| `check_dependencies()`        | Проверяет наличие утилит `curl` и `bc`         |
| `check_internet_connection()` | Проверяет доступность сервиса Cloudflare       |
| `convert_to_mbps()`           | Конвертирует байты/сек в мегабиты/сек          |
| `print_speed_result()`        | Форматирует результат с точностью до сотых     |
| `test_download_speed()`       | Тестирует скорость загрузки (download)         |
| `test_upload_speed()`         | Тестирует скорость отправки (upload)           |
| `show_help()`                 | Выводит справку по использованию               |
| `main()`                      | Основная функция управления потоком выполнения |

---

### 🌐 Принцип работы

1. Проверяется наличие активного интернет-соединения.
2. Проверяются зависимости (`curl`, `bc`).
3. Выполняется **скачивание 10 МБ файла** с Cloudflare (`__down?bytes=10000000`) для замера скорости загрузки.
4. Выполняется **отправка 10 МБ данных** на Cloudflare (`__up`) для замера скорости отправки.
5. Результаты автоматически переводятся в мегабиты в секунду и форматируются в таблицу.

---

### 📊 Пример вывода

```bash
$ ./speedi

🚀 Speedi - Тестер скорости интернет-соединения
================================================

🌐 Проверяем подключение к интернету...
✅ Подключение к интернету установлено

⬇️  Тестируем скорость загрузки...
⚙️  Скорость загрузки :   85.42 Mbit/s

⬆️  Тестируем скорость отправки...
⚙️  Скорость отправки  :   19.87 Mbit/s

✅ Тестирование завершено!
```

---

### 🛠️ Используемые технологии и инструменты

| Компонент                     | Описание                                         |
| ----------------------------- | ------------------------------------------------ |
| **Bash (POSIX shell)**        | Основной язык скрипта                            |
| **curl**                      | Тестирование скорости загрузки и отправки данных |
| **bc**                        | Арифметические вычисления с плавающей точкой     |
| **dd**                        | Генерация данных для теста отправки              |
| **Cloudflare Speed Test API** | Тестовый сервис                                  |

---

### 🔐 Безопасность и стабильность

* Все соединения осуществляются по **HTTPS**, что обеспечивает безопасность передачи данных.
* Для тестирования не используется никаких авторизационных данных.
* В случае отсутствия интернета или зависимостей скрипт завершается с понятным сообщением об ошибке.
* Используется строгий режим `set -e` для предотвращения выполнения при ошибках.

---

### 💡 Возможные направления развития

* ✅ Добавить **опцию вывода в JSON** для интеграции с системами мониторинга.
* ✅ Добавить **логирование результатов** в файл.
* ✅ Поддержка **многократных замеров** и усреднения результатов.
* ✅ Добавить **цветное форматирование вывода** с помощью `tput`.
* ✅ Расширить совместимость с Linux, macOS и WSL.

---

### 🧾 Автор и версия

**Автор:** Владимир Бычко
**Соавтор:** ChatGPT (SourceCraft Code Assistant)
**Версия:** 1.0
**Дата:** 2025
**Лицензия:** MIT

---

### 🚀 Быстрый старт

```bash
# Сделать скрипт исполняемым
chmod +x speedi

# Запустить тест
speedi

# Посмотреть справку
speedi --help
```
