# 🧠 Пояснительная записка

## к shell-утилите для перезагрузки роутера **Keenetic Speedster (KN-3012)**

---

### 📌 Назначение скрипта

Утилита предназначена для **удалённой перезагрузки роутера Keenetic Speedster (KN-3012)** под управлением **KeeneticOS 4.2.6.3** с использованием **Telnet-соединения**.
Решает задачу автоматизации рутинного процесса без необходимости ручного входа в интерфейс роутера.

---

### ⚙️ Принцип работы

1. Проверяется доступность роутера по адресу `192.168.1.1` с помощью команды `ping`.
   Если устройство недоступно, выполнение прекращается.

2. При успешном пинге выполняется подключение по **Telnet** на порт `23`.

3. Авторизация осуществляется под учётной записью:

   ```
   Username: admin
   Password: admin
   ```

4. После успешного входа выполняется команда:

   ```
   system reboot
   ```

   которая инициирует перезагрузку роутера.

5. Скрипт ожидает завершения операции и выводит сообщение о результате.

---

### 🧩 Используемые технологии

| Компонент  | Назначение                                               |
| ---------- | -------------------------------------------------------- |
| **Bash**   | Основная логика скрипта                                  |
| **expect** | Автоматизация ввода команд в интерактивной Telnet-сессии |
| **ping**   | Проверка доступности устройства                          |
| **telnet** | Альтернативный канал выполнения команд                   |
| **mktemp** | Создание временных файлов для промежуточных данных       |

---

### 🔐 Безопасность

* В скрипте используются **открытые учетные данные** (`admin/admin`), что **не рекомендуется** для постоянной эксплуатации.
  Рекомендуется заменить их на индивидуальные логин и пароль.
* При необходимости можно использовать **переменные окружения** или **конфигурационный файл** с ограниченными правами доступа (`chmod 600`).

---

### 🚀 Пример использования

```bash
chmod +x restart-router
restart-router
```

**Результат выполнения:**

```
Перезагрузка роутера Кинетик Speedster (KN-3012) через telnet...
Команда перезагрузки отправлена
Роутер должен перезагрузиться в течение нескольких секунд
```

---

### 💡 Возможные улучшения

* ✅ Поддержка SSH вместо Telnet
* ✅ Чтение параметров (IP, логин, пароль) из конфигурационного файла
* ✅ Логирование событий в отдельный файл
* ✅ Проверка статуса роутера после перезагрузки

---

### 🧾 Автор и версия

**Автор:** Владимир
**Версия:** 1.0
**Дата:** 2025-11-13